apiVersion: apps/v1
kind: Deployment
metadata:
  name: appsku-deploy
spec:
  selector:
    matchLabels:
      apps-type: appsku
  template:
    metadata:
      labels:
        app: appsku
    spec:
      containers:
      - name: ct-appsku
        image: galawana/apps-games
        ports:
        - containerPort: 80
